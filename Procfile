# Procfile for Render deployment
# Specifies how to run the application

web: uvicorn src.api.main:app --host 0.0.0.0 --port $PORT

# Optional: Background workers
# worker: python src/pipeline/scheduler.py
